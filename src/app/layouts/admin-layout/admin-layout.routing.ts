import { Routes } from '@angular/router';
import { HomepageComponent } from 'app/homepage/homepage.component';

export const AdminLayoutRoutes: Routes = [
    { path: 'vagas',        component: HomepageComponent },
];
